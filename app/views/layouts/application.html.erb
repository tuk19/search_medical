<!DOCTYPE html>
<html>
  <head>
    <title>SearchMedical</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script type="text/javascript" src="//jpostal-1006.appspot.com/jquery.jpostal.js"></script>
  </head>
  <body>
    <header class="header">
      <div class="top_bar">
        <h2 class="page_title">医療機関検索</h2>
        <div class="link_home font_large"><%= link_to "home", root_path %></div>
        <div class="link_institution font_large"><%= link_to "医療機関情報", institutions_path %></div>
        <% if user_signed_in? %>
          <%= render "shared/header_user_sign_in" %>
        <% elsif medicalstaff_signed_in? %>
          <%= render "shared/header_medicalstaff_sign_in" %>
        <% end %>
      </div>
    </header>
    <% if flash.notice %>
      <%= flash.notice %>
    <% end %>
    <%= yield %>
    <div class="footer">
    </div>
  </body>
</html>
